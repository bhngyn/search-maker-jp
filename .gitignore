# macOS
.DS_Store
._*
.AppleDouble
.LSOverride

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# Node.js
node_modules/
npm-debug.log

# Vite build output
# (committed only on release tags via `npm run build`)
dist/

# Claude Code agent worktrees (local-only, never tracked)
.claude/worktrees/

# PDF build intermediates (regenerated by scripts/build-pdfs.mjs)
docs/.pdf-tmp/
